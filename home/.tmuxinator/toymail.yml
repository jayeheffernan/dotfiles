# ~/.tmuxinator/toymail.yml

name: toymail
root: ~/Documents/Jobs/Toymail/Toy-Code/v2.1

# Specifies (by name or index) which window will be selected on project startup. If not set, the first window is used.
startup_window: logs

windows:
  - vim:
      panes:
        - vim -c CtrlP
  - logs:
      layout: main-vertical
      panes:
        - 
        - ./logs 30000c2a690c8331
